# `src/fabric_api/apis/` — GENERATED

**Do not hand-edit.** These files are produced from
[`../../openapi.yaml`](../../../openapi.yaml) by openapi-generator's
`python-fastapi` template.

Each `*_api.py` file declares a FastAPI `APIRouter` and the endpoint
signatures; the matching `*_api_base.py` defines the abstract base class
that real implementations in [`../impl/`](../impl/) must subclass.

To regenerate, see [`../../REGENERATE.md`](../../../REGENERATE.md).
