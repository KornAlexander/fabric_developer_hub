// Workload-wide version badge for the Developer Hub (AgentHub workload).
//
// Bump on any user-visible change to the AgentHub shell, navigation,
// item lifecycle integration, or shared services. PBI Fixer has its
// own independent version (`PBI_FIXER_VERSION` in
// `components/PbiFixer/components/PbiFixerPage.tsx`) — bump that one
// separately for Fixer-only changes.
export const WORKLOAD_VERSION = "v1.12";
