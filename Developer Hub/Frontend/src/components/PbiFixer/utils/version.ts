// WS-N: single source of truth for the PBI Fixer version badge.
//
// Bump this on every user-visible PBI Fixer change (per repo memory
// convention: 0.x → 0.x+1; only manual jump to 1.0). The AgentHub
// shell version is independent and lives in `src/version.ts`.
export const PBI_FIXER_VERSION = "v0.37";
